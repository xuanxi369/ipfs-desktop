# Phase D4a — 加密存储设计 Spike（供决策）

> 目的：在动手写加密存储之前，**先把「加密 vs 内容寻址」的取舍讲清楚，给出可拍板的选项**。
> 这是 Phase D4 的前置——[PHASE_D_PLAN.md](PHASE_D_PLAN.md) §5。
>
> 结论先行：**推荐「按用途分流」（选项 C）**——公有内容保持明文可寻址，私有内容静态加密。
> 理由见 §4。

---

## 1. 核心张力：加密与内容寻址天然冲突

IPFS/iroh 的地基是**内容寻址**：`CID = hash(内容)`。这带来两个和加密对冲的性质：

1. **去重与可寻址依赖明文哈希**：`hash(明文)` 才能被网络按 CID 寻址、被他人复用。
   一旦 `hash(密文)`，CID 变成随机值，**无法用明文 CID 分享、无法跨节点去重**。
2. **分享即暴露**：内容寻址的分享语义是「给你 CID，你自己去网络取」。若内容加密，
   光有 CID 取不出明文——必须**额外传密钥**，安全边界从「网络」挪到「密钥分发」。

**因此加密不是给存储层加个开关那么简单，它改变了「什么能被寻址/分享」。**

---

## 2. 三条候选路径

| | **A. 整 store 文件系统级加密** | **B. 逐 blob convergent encryption** | **C. 按用途分流（推荐）** |
|---|---|---|---|
| 思路 | 把 `FsStore` 数据目录整体落在加密卷/加密文件系统上 | 每个 blob 用 `key=hash(明文)` 加密（收敛加密），CID 仍可去重 | 内容分「公有/私有」两类：公有明文可寻址，私有静态加密、默认不出网 |
| 内容寻址 | ✅ 不变（解密后仍是原 CID） | ⚠️ CID 语义改变（存的是密文 CID + 需要 keyinfo） | ✅ 公有不变；私有本就不打算公网寻址 |
| 跨节点分享 | ✅ 公有正常；但「私有」概念缺失 | ⚠️ 需连同收敛密钥一起给 | ✅ 公有给 CID；私有给「CID+密钥」或干脆不分享 |
| 去重 | ✅ | ✅（收敛加密保留去重） | ✅ 公有；私有可选去重 |
| 实现复杂度 | 低（依赖 OS/第三方加密卷） | 高（要改 add/cat 的编解码链） | 中（加一层「私有内容」路由 + 加密 store） |
| 安全边界 | 静态加密整盘；进程运行时全明文 | 每 blob 独立密钥；元数据仍需保护 | 私有内容端到端可控；公有本就公开 |
| 密钥管理 | 一把盘密钥（OS keychain / 节点身份派生） | 每 blob 收敛密钥（可由 manifest 汇总） | 一把「私有主密钥」（节点身份派生 / OS keychain） |
| 与心智模型 | 「我整台机器加密」 | 「我所有内容加密但仍可去重」 | 「我选哪些是私密的」——**最贴近用户直觉** |

---

## 3. 密钥来源（三条路共用的子问题）

- **由节点身份派生**：以 iroh `SecretKey`（`node.secret`）为根，HKDF 派生存储密钥。
  好处：一根信任锚，身份即密钥；坏处：身份密钥泄露 = 存储泄露。
- **OS keychain**：Windows Credential Manager / macOS Keychain / Linux Secret Service。
  好处：与系统安全边界对齐；坏处：跨平台实现差异、无 keychain 环境需回退。
- **推荐**：私有主密钥存 OS keychain，**回退**为「身份派生 + 本地文件（0600 权限）」，
  与既有「私钥不明文落盘」的安全基线一致。

---

## 4. 推荐：选项 C（按用途分流）+ 理由

1. **不破坏内容寻址**：公有内容照旧明文可寻址、可分享、可去重——项目的核心资产不动。
2. **直面真实需求**：用户要的是「**我这些东西别人别看**」，而不是「我全盘加密但还要能公网寻址」
   （后者本身自相矛盾）。分流精确匹配「可信个人节点」的心智：**我掌控我共享什么、对谁共享**。
3. **增量可落地**：在 `Backend`/路由缝上加一个「私有内容」通道（写入加密 store、默认不 serving），
   与现有明文通道并存，**加法式、零回归**。
4. **为 Phase E 留门**：「信任圈内容互备」天然是「私有但对特定人可见」——分流模型正好承接。

**取舍代价（诚实标注）**：私有内容的分享需要「CID + 密钥」双要素，比公有多一步；
私有内容默认不进 DHT/relay，牺牲「全球可寻址」换「可控」。这是**刻意的**。

---

## 5. 若采纳 C，后续 D4b 的落地形状（预览，非本 spike 承诺）

- 新增 `encrypted_store`（feature-gated）：AEAD（如 XChaCha20-Poly1305）加密 blob 落盘。
- 路由层加 `Visibility::{Public, Private}`：`add_file(prefer, visibility)`；私有写加密 store、公有走现有。
- 私有内容分享：`share_private(cid)` → 产出「ticket + 密钥」封装（明确提示这是端到端凭证）。
- 密钥：OS keychain 优先，回退身份派生。

---

## 6. 需要你拍板的决策点

1. **走哪条路？** —— 推荐 **C（按用途分流）**；若你更想要「无脑全加密」可选 A。
2. **密钥来源？** —— 推荐 **OS keychain 优先 + 身份派生回退**。
3. **私有内容默认是否可被 serving/发现？** —— 推荐 **默认否**（私有即私有，显式才对外）。
4. **优先级** —— D4 是否在 D2（长驻）/D3（可观测）之后再实现？（推荐是，D4 设计先挂着）

> 拍板后，D4b 才进入实现。在此之前，加密存储**不写一行落地代码**——避免糊。
