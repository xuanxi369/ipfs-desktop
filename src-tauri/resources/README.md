# Bundled Kubo

Run `npm run setup:kubo` from the repository root to download and verify the
official Windows Kubo binary. The generated `ipfs.exe` is intentionally not
tracked by Git; Tauri includes files in this directory in packaged builds.
